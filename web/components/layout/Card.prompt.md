Base surface. `interactive` adds hover lift for clickable list rows; `glow` adds a gold ring to flag a pépite.

```jsx
<Card interactive onClick={open}>…annonce…</Card>
<Card glow>…top pépite…</Card>
```
