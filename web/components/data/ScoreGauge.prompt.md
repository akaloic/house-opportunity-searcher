The headline pépite score. Color is automatic from the value (red <25 → gold ~50 → teal 80+). Use big on detail views, small (size≈48) in list rows.

```jsx
<ScoreGauge value={87} label="Score pépite" />
<ScoreGauge value={52} size={48} showValue />
```
