Health indicator for the monitoring view. Active states (`online`, `running`, `warning`, `error`) pulse; `idle`/`blocked` are static.

```jsx
<StatusDot status="online" label="Leboncoin · scraper" />
<StatusDot status="blocked" label="Proxy FR-3" />
```
