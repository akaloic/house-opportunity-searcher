Binary toggle for strict filters and alert channels (exclure RDC, alerte mail, mode temps réel).

```jsx
<Switch defaultChecked label="Alerte e-mail instantanée" />
<Switch label="Exclure rez-de-chaussée" />
```
