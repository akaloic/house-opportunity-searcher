Text / number input for filter and config forms. Use `suffix` for units (€, m², min) and `prefix` for a leading icon. Numbers render with tabular figures.

```jsx
<Input type="number" placeholder="450 000" suffix="€" prefix={<Euro/>} />
<Input placeholder="Paris 11e, Montreuil…" />
```
